/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    getStructDecoder,
    getStructEncoder,
    getU8Decoder,
    getU8Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
    type WritableSignerAccount,
} from '@solana/kit';
import {
    getAccountMetaFactory,
    getAddressFromResolvedInstructionAccount,
    type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findSubscriptionAuthorityPda } from '../pdas';
import { SUBSCRIPTIONS_PROGRAM_ADDRESS } from '../programs';

export const REVOKE_SUBSCRIPTION_AUTHORITY_DISCRIMINATOR = 14;

export function getRevokeSubscriptionAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
    return getU8Encoder().encode(REVOKE_SUBSCRIPTION_AUTHORITY_DISCRIMINATOR);
}

export type RevokeSubscriptionAuthorityInstruction<
    TProgram extends string = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
    TAccountUser extends string | AccountMeta<string> = string,
    TAccountUserAta extends string | AccountMeta<string> = string,
    TAccountTokenMint extends string | AccountMeta<string> = string,
    TAccountTokenProgram extends string | AccountMeta<string> = string,
    TAccountSubscriptionAuthority extends string | AccountMeta<string> = string,
    TAccountReceiver extends string | AccountMeta<string> | undefined = undefined,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountUser extends string
                ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
                : TAccountUser,
            TAccountUserAta extends string ? WritableAccount<TAccountUserAta> : TAccountUserAta,
            TAccountTokenMint extends string ? ReadonlyAccount<TAccountTokenMint> : TAccountTokenMint,
            TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram,
            TAccountSubscriptionAuthority extends string
                ? WritableAccount<TAccountSubscriptionAuthority>
                : TAccountSubscriptionAuthority,
            ...(TAccountReceiver extends undefined
                ? []
                : [TAccountReceiver extends string ? WritableAccount<TAccountReceiver> : TAccountReceiver]),
            ...TRemainingAccounts,
        ]
    >;

export type RevokeSubscriptionAuthorityInstructionData = { discriminator: number };

export type RevokeSubscriptionAuthorityInstructionDataArgs = {};

export function getRevokeSubscriptionAuthorityInstructionDataEncoder(): FixedSizeEncoder<RevokeSubscriptionAuthorityInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()]]), value => ({
        ...value,
        discriminator: REVOKE_SUBSCRIPTION_AUTHORITY_DISCRIMINATOR,
    }));
}

export function getRevokeSubscriptionAuthorityInstructionDataDecoder(): FixedSizeDecoder<RevokeSubscriptionAuthorityInstructionData> {
    return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getRevokeSubscriptionAuthorityInstructionDataCodec(): FixedSizeCodec<
    RevokeSubscriptionAuthorityInstructionDataArgs,
    RevokeSubscriptionAuthorityInstructionData
> {
    return combineCodec(
        getRevokeSubscriptionAuthorityInstructionDataEncoder(),
        getRevokeSubscriptionAuthorityInstructionDataDecoder(),
    );
}

export type RevokeSubscriptionAuthorityAsyncInput<
    TAccountUser extends string = string,
    TAccountUserAta extends string = string,
    TAccountTokenMint extends string = string,
    TAccountTokenProgram extends string = string,
    TAccountSubscriptionAuthority extends string = string,
    TAccountReceiver extends string = string,
> = {
    /** The ATA owner: revokes the program's delegate and, when still open, closes the SubscriptionAuthority PDA (receives rent when self-funded) */
    user: TransactionSigner<TAccountUser>;
    /** The user's ATA whose delegate is being revoked */
    userAta: Address<TAccountUserAta>;
    /** The token mint of the user's ATA */
    tokenMint: Address<TAccountTokenMint>;
    /** Token program */
    tokenProgram: Address<TAccountTokenProgram>;
    /** The SubscriptionAuthority PDA. Closed when still open; ignored when already closed. */
    subscriptionAuthority?: Address<TAccountSubscriptionAuthority>;
    /** Optional rent recipient, required when the SubscriptionAuthority is still open and its recorded payer differs from the user. Must match the stored payer. */
    receiver?: Address<TAccountReceiver>;
};

export async function getRevokeSubscriptionAuthorityInstructionAsync<
    TAccountUser extends string,
    TAccountUserAta extends string,
    TAccountTokenMint extends string,
    TAccountTokenProgram extends string,
    TAccountSubscriptionAuthority extends string,
    TAccountReceiver extends string,
    TProgramAddress extends Address = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
>(
    input: RevokeSubscriptionAuthorityAsyncInput<
        TAccountUser,
        TAccountUserAta,
        TAccountTokenMint,
        TAccountTokenProgram,
        TAccountSubscriptionAuthority,
        TAccountReceiver
    >,
    config?: { programAddress?: TProgramAddress },
): Promise<
    RevokeSubscriptionAuthorityInstruction<
        TProgramAddress,
        TAccountUser,
        TAccountUserAta,
        TAccountTokenMint,
        TAccountTokenProgram,
        TAccountSubscriptionAuthority,
        TAccountReceiver
    >
> {
    // Program address.
    const programAddress = config?.programAddress ?? SUBSCRIPTIONS_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        user: { value: input.user ?? null, isWritable: true },
        userAta: { value: input.userAta ?? null, isWritable: true },
        tokenMint: { value: input.tokenMint ?? null, isWritable: false },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
        subscriptionAuthority: { value: input.subscriptionAuthority ?? null, isWritable: true },
        receiver: { value: input.receiver ?? null, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Resolve default values.
    if (!accounts.subscriptionAuthority.value) {
        accounts.subscriptionAuthority.value = await findSubscriptionAuthorityPda({
            user: getAddressFromResolvedInstructionAccount('user', accounts.user.value),
            tokenMint: getAddressFromResolvedInstructionAccount('tokenMint', accounts.tokenMint.value),
        });
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'omitted');
    return Object.freeze({
        accounts: [
            getAccountMeta('user', accounts.user),
            getAccountMeta('userAta', accounts.userAta),
            getAccountMeta('tokenMint', accounts.tokenMint),
            getAccountMeta('tokenProgram', accounts.tokenProgram),
            getAccountMeta('subscriptionAuthority', accounts.subscriptionAuthority),
            getAccountMeta('receiver', accounts.receiver),
        ].filter(<T>(x: T | undefined): x is T => x !== undefined),
        data: getRevokeSubscriptionAuthorityInstructionDataEncoder().encode({}),
        programAddress,
    } as RevokeSubscriptionAuthorityInstruction<
        TProgramAddress,
        TAccountUser,
        TAccountUserAta,
        TAccountTokenMint,
        TAccountTokenProgram,
        TAccountSubscriptionAuthority,
        TAccountReceiver
    >);
}

export type RevokeSubscriptionAuthorityInput<
    TAccountUser extends string = string,
    TAccountUserAta extends string = string,
    TAccountTokenMint extends string = string,
    TAccountTokenProgram extends string = string,
    TAccountSubscriptionAuthority extends string = string,
    TAccountReceiver extends string = string,
> = {
    /** The ATA owner: revokes the program's delegate and, when still open, closes the SubscriptionAuthority PDA (receives rent when self-funded) */
    user: TransactionSigner<TAccountUser>;
    /** The user's ATA whose delegate is being revoked */
    userAta: Address<TAccountUserAta>;
    /** The token mint of the user's ATA */
    tokenMint: Address<TAccountTokenMint>;
    /** Token program */
    tokenProgram: Address<TAccountTokenProgram>;
    /** The SubscriptionAuthority PDA. Closed when still open; ignored when already closed. */
    subscriptionAuthority: Address<TAccountSubscriptionAuthority>;
    /** Optional rent recipient, required when the SubscriptionAuthority is still open and its recorded payer differs from the user. Must match the stored payer. */
    receiver?: Address<TAccountReceiver>;
};

export function getRevokeSubscriptionAuthorityInstruction<
    TAccountUser extends string,
    TAccountUserAta extends string,
    TAccountTokenMint extends string,
    TAccountTokenProgram extends string,
    TAccountSubscriptionAuthority extends string,
    TAccountReceiver extends string,
    TProgramAddress extends Address = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
>(
    input: RevokeSubscriptionAuthorityInput<
        TAccountUser,
        TAccountUserAta,
        TAccountTokenMint,
        TAccountTokenProgram,
        TAccountSubscriptionAuthority,
        TAccountReceiver
    >,
    config?: { programAddress?: TProgramAddress },
): RevokeSubscriptionAuthorityInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserAta,
    TAccountTokenMint,
    TAccountTokenProgram,
    TAccountSubscriptionAuthority,
    TAccountReceiver
> {
    // Program address.
    const programAddress = config?.programAddress ?? SUBSCRIPTIONS_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        user: { value: input.user ?? null, isWritable: true },
        userAta: { value: input.userAta ?? null, isWritable: true },
        tokenMint: { value: input.tokenMint ?? null, isWritable: false },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
        subscriptionAuthority: { value: input.subscriptionAuthority ?? null, isWritable: true },
        receiver: { value: input.receiver ?? null, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    const getAccountMeta = getAccountMetaFactory(programAddress, 'omitted');
    return Object.freeze({
        accounts: [
            getAccountMeta('user', accounts.user),
            getAccountMeta('userAta', accounts.userAta),
            getAccountMeta('tokenMint', accounts.tokenMint),
            getAccountMeta('tokenProgram', accounts.tokenProgram),
            getAccountMeta('subscriptionAuthority', accounts.subscriptionAuthority),
            getAccountMeta('receiver', accounts.receiver),
        ].filter(<T>(x: T | undefined): x is T => x !== undefined),
        data: getRevokeSubscriptionAuthorityInstructionDataEncoder().encode({}),
        programAddress,
    } as RevokeSubscriptionAuthorityInstruction<
        TProgramAddress,
        TAccountUser,
        TAccountUserAta,
        TAccountTokenMint,
        TAccountTokenProgram,
        TAccountSubscriptionAuthority,
        TAccountReceiver
    >);
}

export type ParsedRevokeSubscriptionAuthorityInstruction<
    TProgram extends string = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        /** The ATA owner: revokes the program's delegate and, when still open, closes the SubscriptionAuthority PDA (receives rent when self-funded) */
        user: TAccountMetas[0];
        /** The user's ATA whose delegate is being revoked */
        userAta: TAccountMetas[1];
        /** The token mint of the user's ATA */
        tokenMint: TAccountMetas[2];
        /** Token program */
        tokenProgram: TAccountMetas[3];
        /** The SubscriptionAuthority PDA. Closed when still open; ignored when already closed. */
        subscriptionAuthority: TAccountMetas[4];
        /** Optional rent recipient, required when the SubscriptionAuthority is still open and its recorded payer differs from the user. Must match the stored payer. */
        receiver?: TAccountMetas[5] | undefined;
    };
    data: RevokeSubscriptionAuthorityInstructionData;
};

export function parseRevokeSubscriptionAuthorityInstruction<
    TProgram extends string,
    TAccountMetas extends readonly AccountMeta[],
>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeSubscriptionAuthorityInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 5) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 5,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    let optionalAccountsRemaining = instruction.accounts.length - 5;
    const getNextOptionalAccount = () => {
        if (optionalAccountsRemaining === 0) return undefined;
        optionalAccountsRemaining -= 1;
        return getNextAccount();
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            user: getNextAccount(),
            userAta: getNextAccount(),
            tokenMint: getNextAccount(),
            tokenProgram: getNextAccount(),
            subscriptionAuthority: getNextAccount(),
            receiver: getNextOptionalAccount(),
        },
        data: getRevokeSubscriptionAuthorityInstructionDataDecoder().decode(instruction.data),
    };
}
