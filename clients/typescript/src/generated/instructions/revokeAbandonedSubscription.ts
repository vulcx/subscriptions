/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    getStructDecoder,
    getStructEncoder,
    getU8Decoder,
    getU8Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
    type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { SUBSCRIPTIONS_PROGRAM_ADDRESS } from '../programs';

export const REVOKE_ABANDONED_SUBSCRIPTION_DISCRIMINATOR = 16;

export function getRevokeAbandonedSubscriptionDiscriminatorBytes(): ReadonlyUint8Array {
    return getU8Encoder().encode(REVOKE_ABANDONED_SUBSCRIPTION_DISCRIMINATOR);
}

export type RevokeAbandonedSubscriptionInstruction<
    TProgram extends string = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
    TAccountPayer extends string | AccountMeta<string> = string,
    TAccountSubscriptionAccount extends string | AccountMeta<string> = string,
    TAccountSubscriptionAuthority extends string | AccountMeta<string> = string,
    TAccountPlanPda extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountPayer extends string
                ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
                : TAccountPayer,
            TAccountSubscriptionAccount extends string
                ? WritableAccount<TAccountSubscriptionAccount>
                : TAccountSubscriptionAccount,
            TAccountSubscriptionAuthority extends string
                ? ReadonlyAccount<TAccountSubscriptionAuthority>
                : TAccountSubscriptionAuthority,
            TAccountPlanPda extends string ? ReadonlyAccount<TAccountPlanPda> : TAccountPlanPda,
            ...TRemainingAccounts,
        ]
    >;

export type RevokeAbandonedSubscriptionInstructionData = { discriminator: number };

export type RevokeAbandonedSubscriptionInstructionDataArgs = {};

export function getRevokeAbandonedSubscriptionInstructionDataEncoder(): FixedSizeEncoder<RevokeAbandonedSubscriptionInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()]]), value => ({
        ...value,
        discriminator: REVOKE_ABANDONED_SUBSCRIPTION_DISCRIMINATOR,
    }));
}

export function getRevokeAbandonedSubscriptionInstructionDataDecoder(): FixedSizeDecoder<RevokeAbandonedSubscriptionInstructionData> {
    return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getRevokeAbandonedSubscriptionInstructionDataCodec(): FixedSizeCodec<
    RevokeAbandonedSubscriptionInstructionDataArgs,
    RevokeAbandonedSubscriptionInstructionData
> {
    return combineCodec(
        getRevokeAbandonedSubscriptionInstructionDataEncoder(),
        getRevokeAbandonedSubscriptionInstructionDataDecoder(),
    );
}

export type RevokeAbandonedSubscriptionInput<
    TAccountPayer extends string = string,
    TAccountSubscriptionAccount extends string = string,
    TAccountSubscriptionAuthority extends string = string,
    TAccountPlanPda extends string = string,
> = {
    /** The recorded payer reclaiming rent */
    payer: TransactionSigner<TAccountPayer>;
    /** The abandoned subscription PDA to close */
    subscriptionAccount: Address<TAccountSubscriptionAccount>;
    /** The subscriber's recorded SubscriptionAuthority PDA for the plan's mint (may be closed) */
    subscriptionAuthority: Address<TAccountSubscriptionAuthority>;
    /** The plan the subscription belongs to; provides the mint */
    planPda: Address<TAccountPlanPda>;
};

export function getRevokeAbandonedSubscriptionInstruction<
    TAccountPayer extends string,
    TAccountSubscriptionAccount extends string,
    TAccountSubscriptionAuthority extends string,
    TAccountPlanPda extends string,
    TProgramAddress extends Address = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
>(
    input: RevokeAbandonedSubscriptionInput<
        TAccountPayer,
        TAccountSubscriptionAccount,
        TAccountSubscriptionAuthority,
        TAccountPlanPda
    >,
    config?: { programAddress?: TProgramAddress },
): RevokeAbandonedSubscriptionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountSubscriptionAccount,
    TAccountSubscriptionAuthority,
    TAccountPlanPda
> {
    // Program address.
    const programAddress = config?.programAddress ?? SUBSCRIPTIONS_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        payer: { value: input.payer ?? null, isWritable: true },
        subscriptionAccount: { value: input.subscriptionAccount ?? null, isWritable: true },
        subscriptionAuthority: { value: input.subscriptionAuthority ?? null, isWritable: false },
        planPda: { value: input.planPda ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('payer', accounts.payer),
            getAccountMeta('subscriptionAccount', accounts.subscriptionAccount),
            getAccountMeta('subscriptionAuthority', accounts.subscriptionAuthority),
            getAccountMeta('planPda', accounts.planPda),
        ],
        data: getRevokeAbandonedSubscriptionInstructionDataEncoder().encode({}),
        programAddress,
    } as RevokeAbandonedSubscriptionInstruction<
        TProgramAddress,
        TAccountPayer,
        TAccountSubscriptionAccount,
        TAccountSubscriptionAuthority,
        TAccountPlanPda
    >);
}

export type ParsedRevokeAbandonedSubscriptionInstruction<
    TProgram extends string = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        /** The recorded payer reclaiming rent */
        payer: TAccountMetas[0];
        /** The abandoned subscription PDA to close */
        subscriptionAccount: TAccountMetas[1];
        /** The subscriber's recorded SubscriptionAuthority PDA for the plan's mint (may be closed) */
        subscriptionAuthority: TAccountMetas[2];
        /** The plan the subscription belongs to; provides the mint */
        planPda: TAccountMetas[3];
    };
    data: RevokeAbandonedSubscriptionInstructionData;
};

export function parseRevokeAbandonedSubscriptionInstruction<
    TProgram extends string,
    TAccountMetas extends readonly AccountMeta[],
>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeAbandonedSubscriptionInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 4) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 4,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            payer: getNextAccount(),
            subscriptionAccount: getNextAccount(),
            subscriptionAuthority: getNextAccount(),
            planPda: getNextAccount(),
        },
        data: getRevokeAbandonedSubscriptionInstructionDataDecoder().decode(instruction.data),
    };
}
