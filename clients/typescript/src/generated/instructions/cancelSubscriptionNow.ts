/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    getProgramDerivedAddress,
    getStructDecoder,
    getStructEncoder,
    getU8Decoder,
    getU8Encoder,
    getUtf8Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
} from '@solana/kit';
import {
    getAccountMetaFactory,
    getAddressFromResolvedInstructionAccount,
    type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findSubscriptionDelegationPda } from '../pdas';
import { SUBSCRIPTIONS_PROGRAM_ADDRESS } from '../programs';
import {
    getCancelSubscriptionNowDataDecoder,
    getCancelSubscriptionNowDataEncoder,
    type CancelSubscriptionNowData,
    type CancelSubscriptionNowDataArgs,
} from '../types';

export const CANCEL_SUBSCRIPTION_NOW_DISCRIMINATOR = 17;

export function getCancelSubscriptionNowDiscriminatorBytes(): ReadonlyUint8Array {
    return getU8Encoder().encode(CANCEL_SUBSCRIPTION_NOW_DISCRIMINATOR);
}

export type CancelSubscriptionNowInstruction<
    TProgram extends string = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
    TAccountSubscriber extends string | AccountMeta<string> = string,
    TAccountMerchant extends string | AccountMeta<string> = string,
    TAccountPlanPda extends string | AccountMeta<string> = string,
    TAccountSubscriptionPda extends string | AccountMeta<string> = string,
    TAccountEventAuthority extends string | AccountMeta<string> = string,
    TAccountSelfProgram extends string | AccountMeta<string> = '6DWb3h2mgsGXxJJWkfeK89T1shoJL65Rvu9EM5URfvfA',
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountSubscriber extends string
                ? ReadonlySignerAccount<TAccountSubscriber> & AccountSignerMeta<TAccountSubscriber>
                : TAccountSubscriber,
            TAccountMerchant extends string
                ? ReadonlySignerAccount<TAccountMerchant> & AccountSignerMeta<TAccountMerchant>
                : TAccountMerchant,
            TAccountPlanPda extends string ? ReadonlyAccount<TAccountPlanPda> : TAccountPlanPda,
            TAccountSubscriptionPda extends string ? WritableAccount<TAccountSubscriptionPda> : TAccountSubscriptionPda,
            TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority,
            TAccountSelfProgram extends string ? ReadonlyAccount<TAccountSelfProgram> : TAccountSelfProgram,
            ...TRemainingAccounts,
        ]
    >;

export type CancelSubscriptionNowInstructionData = {
    discriminator: number;
    cancelSubscriptionNowData: CancelSubscriptionNowData;
};

export type CancelSubscriptionNowInstructionDataArgs = { cancelSubscriptionNowData: CancelSubscriptionNowDataArgs };

export function getCancelSubscriptionNowInstructionDataEncoder(): FixedSizeEncoder<CancelSubscriptionNowInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', getU8Encoder()],
            ['cancelSubscriptionNowData', getCancelSubscriptionNowDataEncoder()],
        ]),
        value => ({ ...value, discriminator: CANCEL_SUBSCRIPTION_NOW_DISCRIMINATOR }),
    );
}

export function getCancelSubscriptionNowInstructionDataDecoder(): FixedSizeDecoder<CancelSubscriptionNowInstructionData> {
    return getStructDecoder([
        ['discriminator', getU8Decoder()],
        ['cancelSubscriptionNowData', getCancelSubscriptionNowDataDecoder()],
    ]);
}

export function getCancelSubscriptionNowInstructionDataCodec(): FixedSizeCodec<
    CancelSubscriptionNowInstructionDataArgs,
    CancelSubscriptionNowInstructionData
> {
    return combineCodec(
        getCancelSubscriptionNowInstructionDataEncoder(),
        getCancelSubscriptionNowInstructionDataDecoder(),
    );
}

export type CancelSubscriptionNowAsyncInput<
    TAccountSubscriber extends string = string,
    TAccountMerchant extends string = string,
    TAccountPlanPda extends string = string,
    TAccountSubscriptionPda extends string = string,
    TAccountEventAuthority extends string = string,
    TAccountSelfProgram extends string = string,
> = {
    /** The subscriber cancelling the subscription */
    subscriber: TransactionSigner<TAccountSubscriber>;
    /** The owner of the subscription plan approving immediate cancellation */
    merchant: TransactionSigner<TAccountMerchant>;
    /** The plan PDA for the subscription */
    planPda: Address<TAccountPlanPda>;
    /** The subscription PDA being cancelled immediately */
    subscriptionPda?: Address<TAccountSubscriptionPda>;
    /** The event authority PDA */
    eventAuthority?: Address<TAccountEventAuthority>;
    /** This program (for self-CPI) */
    selfProgram?: Address<TAccountSelfProgram>;
    cancelSubscriptionNowData: CancelSubscriptionNowInstructionDataArgs['cancelSubscriptionNowData'];
};

export async function getCancelSubscriptionNowInstructionAsync<
    TAccountSubscriber extends string,
    TAccountMerchant extends string,
    TAccountPlanPda extends string,
    TAccountSubscriptionPda extends string,
    TAccountEventAuthority extends string,
    TAccountSelfProgram extends string,
    TProgramAddress extends Address = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
>(
    input: CancelSubscriptionNowAsyncInput<
        TAccountSubscriber,
        TAccountMerchant,
        TAccountPlanPda,
        TAccountSubscriptionPda,
        TAccountEventAuthority,
        TAccountSelfProgram
    >,
    config?: { programAddress?: TProgramAddress },
): Promise<
    CancelSubscriptionNowInstruction<
        TProgramAddress,
        TAccountSubscriber,
        TAccountMerchant,
        TAccountPlanPda,
        TAccountSubscriptionPda,
        TAccountEventAuthority,
        TAccountSelfProgram
    >
> {
    // Program address.
    const programAddress = config?.programAddress ?? SUBSCRIPTIONS_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        subscriber: { value: input.subscriber ?? null, isWritable: false },
        merchant: { value: input.merchant ?? null, isWritable: false },
        planPda: { value: input.planPda ?? null, isWritable: false },
        subscriptionPda: { value: input.subscriptionPda ?? null, isWritable: true },
        eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
        selfProgram: { value: input.selfProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    // Resolve default values.
    if (!accounts.subscriptionPda.value) {
        accounts.subscriptionPda.value = await findSubscriptionDelegationPda({
            planPda: getAddressFromResolvedInstructionAccount('planPda', accounts.planPda.value),
            subscriber: getAddressFromResolvedInstructionAccount('subscriber', accounts.subscriber.value),
        });
    }
    if (!accounts.eventAuthority.value) {
        accounts.eventAuthority.value = await getProgramDerivedAddress({
            programAddress,
            seeds: [getUtf8Encoder().encode('event_authority')],
        });
    }
    if (!accounts.selfProgram.value) {
        accounts.selfProgram.value = programAddress;
        accounts.selfProgram.isWritable = false;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('subscriber', accounts.subscriber),
            getAccountMeta('merchant', accounts.merchant),
            getAccountMeta('planPda', accounts.planPda),
            getAccountMeta('subscriptionPda', accounts.subscriptionPda),
            getAccountMeta('eventAuthority', accounts.eventAuthority),
            getAccountMeta('selfProgram', accounts.selfProgram),
        ],
        data: getCancelSubscriptionNowInstructionDataEncoder().encode(args as CancelSubscriptionNowInstructionDataArgs),
        programAddress,
    } as CancelSubscriptionNowInstruction<
        TProgramAddress,
        TAccountSubscriber,
        TAccountMerchant,
        TAccountPlanPda,
        TAccountSubscriptionPda,
        TAccountEventAuthority,
        TAccountSelfProgram
    >);
}

export type CancelSubscriptionNowInput<
    TAccountSubscriber extends string = string,
    TAccountMerchant extends string = string,
    TAccountPlanPda extends string = string,
    TAccountSubscriptionPda extends string = string,
    TAccountEventAuthority extends string = string,
    TAccountSelfProgram extends string = string,
> = {
    /** The subscriber cancelling the subscription */
    subscriber: TransactionSigner<TAccountSubscriber>;
    /** The owner of the subscription plan approving immediate cancellation */
    merchant: TransactionSigner<TAccountMerchant>;
    /** The plan PDA for the subscription */
    planPda: Address<TAccountPlanPda>;
    /** The subscription PDA being cancelled immediately */
    subscriptionPda: Address<TAccountSubscriptionPda>;
    /** The event authority PDA */
    eventAuthority: Address<TAccountEventAuthority>;
    /** This program (for self-CPI) */
    selfProgram?: Address<TAccountSelfProgram>;
    cancelSubscriptionNowData: CancelSubscriptionNowInstructionDataArgs['cancelSubscriptionNowData'];
};

export function getCancelSubscriptionNowInstruction<
    TAccountSubscriber extends string,
    TAccountMerchant extends string,
    TAccountPlanPda extends string,
    TAccountSubscriptionPda extends string,
    TAccountEventAuthority extends string,
    TAccountSelfProgram extends string,
    TProgramAddress extends Address = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
>(
    input: CancelSubscriptionNowInput<
        TAccountSubscriber,
        TAccountMerchant,
        TAccountPlanPda,
        TAccountSubscriptionPda,
        TAccountEventAuthority,
        TAccountSelfProgram
    >,
    config?: { programAddress?: TProgramAddress },
): CancelSubscriptionNowInstruction<
    TProgramAddress,
    TAccountSubscriber,
    TAccountMerchant,
    TAccountPlanPda,
    TAccountSubscriptionPda,
    TAccountEventAuthority,
    TAccountSelfProgram
> {
    // Program address.
    const programAddress = config?.programAddress ?? SUBSCRIPTIONS_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        subscriber: { value: input.subscriber ?? null, isWritable: false },
        merchant: { value: input.merchant ?? null, isWritable: false },
        planPda: { value: input.planPda ?? null, isWritable: false },
        subscriptionPda: { value: input.subscriptionPda ?? null, isWritable: true },
        eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
        selfProgram: { value: input.selfProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    // Resolve default values.
    if (!accounts.selfProgram.value) {
        accounts.selfProgram.value = programAddress;
        accounts.selfProgram.isWritable = false;
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('subscriber', accounts.subscriber),
            getAccountMeta('merchant', accounts.merchant),
            getAccountMeta('planPda', accounts.planPda),
            getAccountMeta('subscriptionPda', accounts.subscriptionPda),
            getAccountMeta('eventAuthority', accounts.eventAuthority),
            getAccountMeta('selfProgram', accounts.selfProgram),
        ],
        data: getCancelSubscriptionNowInstructionDataEncoder().encode(args as CancelSubscriptionNowInstructionDataArgs),
        programAddress,
    } as CancelSubscriptionNowInstruction<
        TProgramAddress,
        TAccountSubscriber,
        TAccountMerchant,
        TAccountPlanPda,
        TAccountSubscriptionPda,
        TAccountEventAuthority,
        TAccountSelfProgram
    >);
}

export type ParsedCancelSubscriptionNowInstruction<
    TProgram extends string = typeof SUBSCRIPTIONS_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        /** The subscriber cancelling the subscription */
        subscriber: TAccountMetas[0];
        /** The owner of the subscription plan approving immediate cancellation */
        merchant: TAccountMetas[1];
        /** The plan PDA for the subscription */
        planPda: TAccountMetas[2];
        /** The subscription PDA being cancelled immediately */
        subscriptionPda: TAccountMetas[3];
        /** The event authority PDA */
        eventAuthority: TAccountMetas[4];
        /** This program (for self-CPI) */
        selfProgram: TAccountMetas[5];
    };
    data: CancelSubscriptionNowInstructionData;
};

export function parseCancelSubscriptionNowInstruction<
    TProgram extends string,
    TAccountMetas extends readonly AccountMeta[],
>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelSubscriptionNowInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 6) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 6,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            subscriber: getNextAccount(),
            merchant: getNextAccount(),
            planPda: getNextAccount(),
            subscriptionPda: getNextAccount(),
            eventAuthority: getNextAccount(),
            selfProgram: getNextAccount(),
        },
        data: getCancelSubscriptionNowInstructionDataDecoder().decode(instruction.data),
    };
}
