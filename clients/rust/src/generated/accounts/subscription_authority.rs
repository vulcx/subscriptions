//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SubscriptionAuthority {
    pub discriminator: u8,
    pub user: Address,
    pub token_mint: Address,
    pub payer: Address,
    pub bump: u8,
    pub init_id: i64,
}

impl SubscriptionAuthority {
    /// Prefix values used to generate a PDA for this account.
    ///
    /// Values are positional and appear in the following order:
    ///
    ///   0. `SubscriptionAuthority::PREFIX`
    ///   1. user (`Address`)
    ///   2. token_mint (`Address`)
    pub const PREFIX: &'static [u8] = "SubscriptionAuthority".as_bytes();

    pub fn create_pda(
        user: Address,
        token_mint: Address,
        bump: u8,
    ) -> Result<solana_address::Address, solana_address::error::AddressError> {
        solana_address::Address::create_program_address(
            &["SubscriptionAuthority".as_bytes(), user.as_ref(), token_mint.as_ref(), &[bump]],
            &crate::SUBSCRIPTIONS_ID,
        )
    }

    pub fn find_pda(user: &Address, token_mint: &Address) -> (solana_address::Address, u8) {
        solana_address::Address::find_program_address(
            &["SubscriptionAuthority".as_bytes(), user.as_ref(), token_mint.as_ref()],
            &crate::SUBSCRIPTIONS_ID,
        )
    }

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for SubscriptionAuthority {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_subscription_authority(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<SubscriptionAuthority>, std::io::Error> {
    let accounts = fetch_all_subscription_authority(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_subscription_authority(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<SubscriptionAuthority>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses).map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<SubscriptionAuthority>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::other(format!("Account not found: {address}")))?;
        let data = SubscriptionAuthority::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_subscription_authority(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<SubscriptionAuthority>, std::io::Error> {
    let accounts = fetch_all_maybe_subscription_authority(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_subscription_authority(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<SubscriptionAuthority>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses).map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<SubscriptionAuthority>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = SubscriptionAuthority::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount {
                address,
                account: account.clone(),
                data,
            }));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}
